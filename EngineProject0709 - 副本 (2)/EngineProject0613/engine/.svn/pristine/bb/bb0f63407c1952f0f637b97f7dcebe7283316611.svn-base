#pragma once

#include<string>
using namespace std;

enum TextureLoad
{
	ERR_DIR = 0,
	ERR_LOAD,
	LOAD_SUCESSE,
};


unsigned int TextureFromFile(const char *path, const string &directory, int &state, bool gamma = false);