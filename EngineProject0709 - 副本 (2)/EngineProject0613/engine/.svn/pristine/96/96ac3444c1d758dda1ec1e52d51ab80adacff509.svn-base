#pragma once

#include<string>
#include"module.h"
using namespace std;

enum TextureLoad
{
	ERR_DIR = 0,
	ERR_LOAD,
	LOAD_SUCESSE,
};


unsigned int TextureFromFile(RenderHelper * r_helper,const char *path, const string &directory, int &state, bool gamma = false);